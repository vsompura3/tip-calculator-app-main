// function calculateTip(billAmount,tipRate,numOfPeople = 1) {
//   const tipPerPerson =( billAmount *  (tipRate / 100) ) / numOfPeople;
//   const totalPerPerson = (billAmount + (billAmount * (tipRate / 100))) / numOfPeople;
//   console.log(tipPerPerson, totalPerPerson);

//   return { tipPerPerson, totalPerPerson };
// }

// const tip = document.querySelector('.calculated-tip');
// const total = document.querySelector('.calculated-total');

// function displayCalculation(x) {
//   const tipPerPerson = x.tipPerPerson;
//   const totalPerPerson = x.totalPerPerson;
//   console.log(tipPerPerson,totalPerPerson);
//   tip.textContent =`$${tipPerPerson.toFixed(2)}` ;
//   total.textContent = `$${totalPerPerson.toFixed(2)}`;
// }

// const a = calculateTip(500,20,2);
// displayCalculation(a);

/* 
=
*/
